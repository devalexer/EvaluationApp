@model IEnumerable<EvaluationApp.Models.Courses>

@{
    ViewData["Title"] = "Index";
}

<div id="mySidenav" class="sidenav">
    <center><a href="/Home/Index"><img src="~/images/Evaluchart3 wht-02.svg" alt="Logo" style="height:100px;width:160px" id="portallogo"></a></center>
    <br />
    <a href="/Home/Index">Home</a>
    <a href="/Home/Index">Back</a>

    <form asp-area="" asp-controller="Account" asp-action="Logout" method="post" id="logoutForm" class="navbar-right">
        <button type="submit" class="btn btn-link navbar-btn navbar-link" id="logOutButton">Log-out</button>
    </form>

</div>

<!-- Add all page content inside this div if you want the side nav to push page content to the right (not used if you only want the sidenav to sit on top of the page) -->
<div id="main">

    <h1 id="portaltitle">Courses</h1>

    <p>
        <button type="Button" class="createNewButton" onclick="location.href='@Url.Action("Create", "Courses")'">
            Create New
        </button>
    </p>

    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th>
                    Course Code
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                        <td>
                            <a href="/Lectures/Index/@item.Id">
                                @Html.DisplayFor(modelItem => item.Name)
                            </a>
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.CourseCode)
                        </td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                        <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

    @*<div class="portalclasses">
        <div class="row">
            <div class="col-sm-2 newplaceholder" href="#">
                <a href="@Url.Action("Details", "Courses", "1")">
                    <img src="/images/blackbluelecture.jpg" width="200" height="200" class="img-responsive" alt="Generic placeholder thumbnail">
                    <h4>Class Name</h4>
                    <span>Click for Access</span>
                </a>
            </div>
            <div class="col-sm-2 newplaceholder">
                <a href="@Url.Action("Index", "Courses")">
                    <img src="/images/blackbluelecture.jpg" width="200" height="200" class="img-responsive" alt="Generic placeholder thumbnail">
                    <h4>Class Name</h4>
                    <span>Click for Access</span>
                </a>
            </div>
        </div>
    </div>*@



